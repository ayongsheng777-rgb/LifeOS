# 03 功能测试报告 — LifeOS V2.0

> 测试方式：真实登录（本地计算 TOTP 动态码）后按业务闭环实测，临时数据测完即清理。

## 测试总览

| 类别 | 通过/总数 | 说明 |
|---|---|---|
| 业务闭环 | 18/18 | 全部通过（含补测修正脚本路由误测项） |
| 异常输入 | 2/2 | 非法参数、负数金额均被 422 拒绝 |

## 一、待办业务闭环 ✅

| 步骤 | 结果 | 证据 |
|---|---|---|
| 创建 | PASS | POST /api/todos → 200，39ms，返回 id |
| 查询 | PASS | GET /api/todos → items 列表含新条目 |
| 完成 | PASS | POST /api/todos/{id}/done → done=true 且 done_at 落库，列表回读一致 |
| 删除 | PASS | DELETE → 200，再查不可见 |

## 二、记账业务闭环 ✅

| 步骤 | 结果 | 证据 |
|---|---|---|
| 创建 | PASS | POST /api/expense（0.01 元测试单）→ 200，36ms |
| 查询 | PASS | GET /api/expense → items 含新记录 |
| 月度汇总 | PASS | GET /api/expense/summary → `{"month":"2026-08","expense":0.01,"count":1}` 数据一致 |
| 删除 | PASS | DELETE → 200 |
| 负数金额 | PASS | 422 拒绝，金额校验生效 |

## 三、记忆系统闭环 ✅

| 项 | 结果 | 证据 |
|---|---|---|
| 事实写入 | PASS | POST /api/memory/fact（key/value/category/source）→ ok |
| 事实查询 | PASS | GET /api/memory/facts 回读到测试值（库中已有真实事实，如用药记录） |
| 事实删除 | PASS | DELETE → ok |
| 短期记忆 | PASS | GET /api/memory/short → working(last_intent) + redis 短记忆列表正常 |
| 长期记忆 | PASS | GET /api/memory/long → qdrant configured=true（当前 items 为空，正常） |

## 四、AI Agent 闭环 ✅

| 项 | 结果 | 证据 |
|---|---|---|
| 对话 | PASS | POST /api/agent/chat「你好」→ 正常回复，22984ms（模型侧延迟，见性能报告） |
| 历史 | PASS | GET /api/agent/history → 200 |
| 技能列表 | PASS | GET /api/skills → 含内置「高德地图」等技能 |
| 模型连通 | PASS | GET /api/ai/probe → z-ai/glm-5.2 ok=true，9.6s |

## 五、配置与备份闭环 ✅

| 项 | 结果 | 证据 |
|---|---|---|
| 系统状态 | PASS | GET /api/status → ok，ai_available=true |
| 配置读取 | PASS | GET /api/config → 密钥已脱敏，无明文 sk- 泄露 |
| 备份状态 | PASS | GET /api/backup/status → targets 配置返回正常 |
| 备份配置读取 | PASS | GET /api/backup/config → 200 |
| 备份日志 | PASS | GET /api/backup/log?since=0 → 200 |
| AI 用量 | PASS | /api/ai/usage（calls=14, fail=0）与 /api/ai/usage/daily（14 天曲线）均正常 |

## 六、未覆盖项（如实声明）

- 备份**还原**（/api/backup/restore）属高危操作，按规范「不主动修改系统」未实测，仅代码审查
- 飞书扫码绑定全流程需手机配合，未实测（/api/feishu/status 显示 bot_online=true）
- 技能代码包上传执行未实测（安全角度不建议在无沙箱环境演示，见安全报告 P1-1）
- 备份实际传输到 SMB/SFTP 目标未实测（调度线程与配置读取正常）

**功能评分：88/100** —— 已测业务闭环全部数据一致，扣分在未覆盖项与个别端点语义（/api/models/active 仅 POST 无 GET）。
